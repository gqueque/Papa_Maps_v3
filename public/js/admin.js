// Aguarda o HTML carregar completamente
document.addEventListener('DOMContentLoaded', () => {
    console.log("Painel de Admin carregado!");

    // Futuramente, a lógica para buscar os dados do Firestore e preencher o dashboard virá aqui.
    // Exemplo:
    // fetchKPIs();
    // initializeHeatmap();
    // fetchTopCities();
});